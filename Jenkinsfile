pipeline {
   agent any 
    stages {
      stage('Installing nodeJS & Starting nodeJS Application on port:3000'){ 
        steps{         
          sh 'npm install'  
          sh 'npm start' 
        }
      }       
    }
}
